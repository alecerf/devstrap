version: "2"

linters:
  default: all
  disable:
    - wsl
    - depguard
    - exhaustruct
    - gochecknoglobals
    - tagliatelle
  settings:
    gosec:
      excludes:
        - G204

formatters:
  enable:
    - gci
    - gofmt
    - gofumpt
    - goimports
    - golines
    - swaggo
